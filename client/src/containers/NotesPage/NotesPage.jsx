import { Box } from "@mui/material";

export default function NotesPage() {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        marginTop: "92px",
        width: "100%",
      }}
    >
      NotesPage
    </Box>
  );
}
